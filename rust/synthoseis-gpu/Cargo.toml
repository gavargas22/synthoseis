[package]
name = "synthoseis-gpu"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Future GPU acceleration hooks (CUDA/Vulkan/etc.); CPU path remains default."

[dependencies]
